name: flare_network_codegen
description: >-
  Development-only CLI that generates typed Dart contract bindings from Flare's
  published periphery ABI artifacts. Not a runtime dependency of anything.
version: 0.1.0-dev.1
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  args: ^2.7.0
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.2
